-Sesn[
	-T[2026_0719_163835][
		先看skill。
		然後看詞典頁。
		現在這個詞典 如果要同時學多門語言、
		頻繁切換源語言的話會比較麻煩。
		比如我同時學五門語言、
		然後查字典的時候經常要換源語言。
		我就把我所學的語言的權重調高、
		然後切換源語言的時候點擊源語言按鈕、
		再點X按鈕清空。這時會按權重顯示語言列表。
		我再在列表中選擇我要的語言。
		這樣我切換語言就要點三下。感覺還是不夠方便。
		你有沒有甚麼想法
	][
````md
看完目前詞典頁與語言選擇頁後，問題很明確：

- 點源語言後，[ViewDictionary.Impl.cs](E:/_code/CsNgan.Dict/Ngan.Dict.Frontend/proj/Ngan.Dict.Ui/Views/Dictionary/ViewDictionary.Impl.cs:235) 會把當前源語言代碼預填進搜索框。
- 選擇頁載入時立即按此代碼搜索，因此只顯示當前語言。
- 必須清空後，才會顯示按權重排序的完整列表。

我想到幾種方案。

**方案一：源語言快速彈出列表，我最推薦**

點源語言按鈕後，不再跳轉完整管理頁，而是在按鈕附近彈出一個輕量列表：

```text
✓ en  英語
  ja  日語
  de  德語
  fr  法語
  es  西班牙語
────────────
  搜索更多語言…
```

操作變成：

```text
點源語言 → 點目標語言
```

兩次點擊。

特點：

- 前幾項直接使用現有 `Weight` 排序，正好符合你的使用習慣。
- 當前語言加勾選標記，但不因它篩掉其他語言。
- 「搜索更多語言」才進入完整 `ViewNormLangPage`。
- 完整頁進入選擇模式時搜索框默認為空，不再預填當前語言。
- 不改變語言管理頁原有的搜索、分頁和新增功能。

這是空間占用、操作效率、可發現性最平衡的方案。

**方案二：常用語言直接常駐詞典頁**

把源語言區改成可橫向捲動的語言籤：

```text
[EN] [JA] [DE] [FR] [ES]  [更多]
```

直接點目標語言，一次完成切換。

優點是最快；缺點是：

- 手機頁頂部會再占一行。
- 五門語言尚可，更多語言時需要橫向捲動。
- 源語言、交換按鈕、目標語言之間的視覺關係可能變得複雜。

如果「頻繁切換」是詞典頁最核心的操作，我會考慮這個方案。

**方案三：點擊源語言循環切換**

每點一次，按權重在常用語言間循環；長按或其他入口打開完整列表。

優點是理想情況只點一次。問題是五門語言時，切到較遠的語言可能要點四次，而且容易誤切、可發現性也差。我不推薦作主要方案。

還可以附加「輸入文字自動猜源語言」，但它只能可靠區分日文、韓文、西里爾字母等不同文字系統；英法德西等拉丁字母語言容易猜錯，不適合作為主要交互。

我的建議是采用方案一：源語言按鈕弹出“按權重排序的快速列表”，完整选择页作为“搜索更多”后备入口。这样由三次点击降为两次，也不长期占用页面空间。

需要你先决定一个关键方向：你更看重“两次点击但页面简洁”的快速弹出列表，还是“一次点击但常驻占一行”的语言快捷栏？
````
	]
	
	-T[2026_0719_164358][
		方案二和我腦中預想的方案比較接近。
		那就在語言選擇欄上面再加一欄。
		先定義一個 NormLangTag 、
		
		````
		橫向GridStack{
			橫向滾動{
				多個ViewNormLangTag
			}
			編輯按鈕[點擊後進入編輯頁面]
		}
		````
		點了之後直接把源語言換成選的語言。目標語言不變。
		
		具體細節沒想好:
			- 編輯頁面怎麼做
			- NormLangTag顯示甚麼東西
			- 持久化的數據存哪裏
			- 其他
		
		你覺得呢?
	][
````md
我認為這個方向是對的，但應明確把它定義成「詞典源語言快捷欄」，不要直接復用 `PoNormLang.Weight` 或“最近使用語言”。

原因是：

- `PoNormLang.Weight` 控制整個標準語言列表的排序，不只服務詞典快捷欄。
- 快捷欄需要“是否顯示、顯示順序”，這是另一份用戶偏好。
- 現有代碼雖曾預留 `RecentUsedNormLangs`，但“最近用過”會自動變動；你的需求更像用戶主動維護的一組固定語言。

我推薦如下设计。

**NormLangTag**

名称上我更倾向：

- 数据/VM：`VmDictionarySrcLangTag`
- 视图：`ViewDictionarySrcLangTag`

而不是泛化的 `NormLangTag`，因为它目前明确只负责词典源语言快捷切换。除非你已经确定其他页面也会复用，才命名为 `ViewNormLangTag`。

Tag 建议显示：

```text
┌─────────────┐
│ JA  日本語  │
└─────────────┘
```

- 语言代码作为最醒目的短文本，例如 `JA`。
- 后面显示本地名称 `日本語`，而非界面翻译名“日语”。
- 当前源语言使用主色背景或边框突出。
- 不显示权重。权重是排序参数，不是用户切换时关心的信息。
- 文本过长时截断，Tag 保持单行。
- 点击后直接调用现有 `ApplySrcNormLang`，目标语言不变。
- 点击当前语言时不执行持久化，避免无意义写入。

本地名称比翻译名称更适合快捷栏：五个 Tag 连续排列时，`EN English / JA 日本語 / DE Deutsch` 的辨识度更高，也不会因 UI 语言切换而改变用户已经熟悉的位置和文本。

**詞典頁布局**

建议采用：

```text
Root
├─ 源語言快捷欄
│  └─ 橫向 GridStack
│     ├─ ScrollViewer
│     │  └─ 橫向 GridStack
│     │     ├─ ViewDictionarySrcLangTag
│     │     ├─ ViewDictionarySrcLangTag
│     │     └─ ...
│     └─ 編輯按鈕
├─ 原有源語言 ⇄ 目標語言
├─ 搜索工具列
└─ 結果區
```

几个细节：

- 编辑按钮固定在右侧，不随横向列表滚走。
- 快捷栏只横向滚动，隐藏竖向滚动条。
- 当前源语言 Tag 自动滚入可见范围是加分项，但首版可不做。
- 原有源语言按钮继续保留，负责从完整语言列表临时选择任意语言。
- 从完整列表选到一个未加入快捷栏的语言时，只切换当前源语言，不自动加入快捷栏，避免列表逐渐污染。

**持久化模型**

建议新增独立配置，例如：

```text
KeysKv.Dictionary.SrcLangShortcuts
```

值存 JSON，但只保存稳定身份和顺序：

```json
[
  { "Type": "Bcp47", "Code": "en" },
  { "Type": "Bcp47", "Code": "ja" },
  { "Type": "Bcp47", "Code": "de" }
]
```

不要保存：

- `PoNormLang.Id`：代码注释已明确说它不适合作持久关联。
- `NativeName`、`EnglishName`、`Weight`：这些应在加载时按 `(Type, Code)` 查询最新 `PoNormLang`。
- 单独的排序数字：JSON 数组顺序已经表达顺序。

我倾向放在 `KeysKv`，跟现有 `CurSrcLang`、`CurTgtLang` 一样按用户同步，而不是放 `KeysClientKv`。这样用户换设备后仍保留快捷栏。如果你明确希望每台设备独立配置，才放 `KeysClientKv`。

**編輯頁**

建议不要复用现有 `ViewNormLangPage` 直接编辑，因为它是分页搜索与标准语言管理页，不适合编辑一个有序集合。

新建专用页面：

```text
ViewDictionarySrcLangShortcutEdit
```

页面结构可以是：

```text
快捷語言
┌─────────────────────────────┐
│ ≡  EN  English         删除 │
│ ≡  JA  日本語          删除 │
│ ≡  DE  Deutsch         删除 │
└─────────────────────────────┘

[添加語言]
```

行为：

- “添加语言”进入现有 `ViewNormLangPage` 选择模式。
- 已添加的语言不能重复添加。
- 支持删除。
- 支持调整顺序。
- 保存后返回词典页并刷新快捷栏。
- 至少保留一个快捷语言，还是允许清空，需要你决定。

排序交互有三种：

1. 上移/下移按钮：实现最稳，移动端也明确。
2. 拖拽排序：体验最好，但 Avalonia 移动端处理成本和误触风险更高。
3. 继续使用 `Weight`：不推荐，用户还要输入数字，而且又把两个概念混在一起。

我推荐首版使用上移/下移，未来再替换为拖拽。

**默认与边界**

我的建议：

- 首次没有配置时：以当前源语言作为唯一快捷项。
- 上限：建议 `8` 或 `12` 个，避免用户误把它当完整语言表。
- 删除当前源语言的快捷项：只从快捷栏移除，不改变当前源语言。
- 标准语言被删除：加载时忽略失效项，并在下次保存时清理。
- 添加重复语言：阻止添加并提示。
- 点击 Tag 切换成功后：更新现有源语言显示及持久化值；目标语言不变。
- 快捷栏编辑结果应跨设备同步：如果你同意，就放用户 KV。

**需要你先裁定的三个问题**

1. 快捷栏配置要跨设备同步吗？我推荐“跨设备”，存用户 `KeysKv`。
2. 编辑页排序首版使用“上移/下移”还是直接做拖拽？我推荐先用上移/下移。
3. Tag 显示采用 `代码 + NativeName`，例如 `JA 日本語`，你是否同意？

这三个确认后，我再整理正式需求确认单；确认需求后才进入多方案设计和编码。
````
	]
	
	-T[2026_0719_170235][
		我覺得Tag的顯示最好弄短點、弄成可編輯的。
		沒必要設上限。
		不編輯就默認顯示Code。
		按所需最小寬度顯示。
		我覺得配置寫進配置文件裏好點、不想寫到Kv去。
	][
````md
這樣定義更合理。快捷欄配置屬於裝置本地 UI 偏好，放配置文件即可，不必進數據庫或 KV。

建議配置結構按陣列順序保存：

```jsonc
{
  "Dictionary": {
    "SrcLangTags": [
      {
        "Type": "Bcp47",
        "Code": "en",
        "Text": "E"
      },
      {
        "Type": "Bcp47",
        "Code": "ja",
        "Text": "日"
      },
      {
        "Type": "Bcp47",
        "Code": "de",
        "Text": ""
      }
    ]
  }
}
```

显示规则：

```text
Text 非空 → 显示 Text
Text 为空 → 显示 Code
```

因此上例显示：

```text
[E] [日] [de]
```

Tag 不设置固定宽度和等分列，使用内容所需最小宽度；当前源语言使用主色背景或边框。自定义文字只负责显示，不参与语言识别，真正身份始终是 `(Type, Code)`。

建议编辑页每行如下：

```text
[↑] [↓]  ja 日本語  [短標籤: 日]  [刪除]
```

底部提供：

```text
[添加語言]
```

- 添加时进入现有标准语言选择页。
- 不允许重复添加同一个 `(Type, Code)`。
- 顺序直接采用配置数组顺序。
- 标签允许留空，保存时去掉首尾空白。
- 删除当前源语言的 Tag 不改变当前源语言。
- 快捷栏允许完全清空；清空后页面只剩右侧编辑按钮。
- 不设置数量上限。

还有三个细节需要你裁定：

1. 我推荐短标签只在专用编辑页修改，不支持在词典页直接长按或双击编辑，可以吗？
2. 排序首版用明确的上移、下移按钮，还是直接做拖拽？我推荐先用上移、下移。
3. 首次没有配置时，快捷栏默认如何生成？我倾向只加入当前源语言；另一种是读取 `PoNormLang.Weight` 最高的若干项，但“若干”又需要默认数量规则。
````
	]
	
	-T[2026_0719_171752][
		1 可。
		2 用上下移。
		3 沒配置就沒配置 不搞特殊行爲
	][
		
	]
]
